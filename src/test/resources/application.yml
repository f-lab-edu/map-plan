spring:
  datasource:
    url: jdbc:h2:mem:test
    username: sa
    password:
    driver-class-name: org.h2.Driver
  mail:
    host: smtp.gmail.com
    port: 587
    username: setset
    password: setset
    properties:
      mail:
        smtp:
          auth: true
          timeout: 5000
          starttls:
            enable: true
  s3:
    accessKey: test
    secretKey: stet
    bucketName: sets
  sql:
    init:
      data-locations:
      mode: never
  jpa:
    hibernate:
      ddl-auto: none

file:
  dir: "/test"